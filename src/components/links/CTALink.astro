---
import { Icon } from 'astro-icon/components';

interface Props {
  label: string;
  href: string;
  outline?: boolean;
  rounded?: boolean;
  icon?: string;
}

const { label, href, outline = false, rounded = false, icon } = Astro.props;

const cssOutline = outline
  ? 'bg-green-500 bg-opacity-50 text-white hover:bg-white hover:text-black'
  : 'bg-green-500 text-white hover:bg-green-600';
---

<a
  class:list={[
    { 'rounded-full': rounded },
    { 'flex items-center gap-2': icon },
    `w-fit font-medium whitespace-nowrap no-underline	px-3 group py-2 text-lg md:text-xl transition-all md:px-6 md:py-3 ${cssOutline}`,
  ]}
  href={href}
  aria-label={label}
  title={label}
>
  {label}
  {
    icon ? (
      <Icon
        name={icon}
        class='group-hover:translate-x-1 md:group-hover:translate-x-3 transition-all duration-300 h-6 md:h-8'
      />
    ) : null
  }
</a>
